// PostgreSQL desabilitado - não é necessário para o momento
// import postgres from 'postgres';

// export const sql = postgres(process.env.POSTGRES_URL!, {
//   ssl: 'allow',
// });

export const sql = null;
